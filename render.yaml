services:
  - type: web
    name: neo4j
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NEO4J_AUTH
        value: neo4j/yourpassword
    disk:
      name: neo4j-data
      mountPath: /var/lib/neo4j/data
      sizeGB: 10
